# MongoDB connection string
MONGO_URI=mongodb://localhost:27017

# Optional: a database to always show in the list, even before it has any data
MONGO_DB=

# Show exception details on error pages (development only)
APP_DEBUG=false
